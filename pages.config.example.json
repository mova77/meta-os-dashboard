{
  "_comment": "Used by .github/workflows/pages.yml to clone your instance vault and federated vault repos into a build-time tree. Copy to pages.config.json and adjust paths. Private repos need the default GITHUB_TOKEN (same org) or a PAT in secrets.PAGES_CLONE_TOKEN.",
  "workDir": ".pages",
  "repos": [
    { "repo": "meta-agentic/mova-os", "dir": "instance" },
    { "repo": "meta-agentic/meta-os", "dir": "framework" },
    { "repo": "mova77/vault-ios", "dir": "vaults/ios" },
    { "repo": "mova77/vault-iam", "dir": "vaults/iam" }
  ],
  "instanceConfig": {
    "instanceRoot": "${workDir}/instance",
    "frameworkRoot": "${workDir}/framework",
    "backlogs": []
  }
}